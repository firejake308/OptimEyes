import React from 'react';
import { Text } from 'react-native';

export default class ItemListPage extends React.Component {
    render() {
        return (
            <Text>This is the future home of the item list page.</Text>
        );
    }
}